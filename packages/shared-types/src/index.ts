// Tipos compartilhados entre api, mobile, web e desktop.
// Ideia: manter isso em sincronia com o schema.prisma (services/api/prisma/schema.prisma)
// pra nunca mais ter "Jogador" definido de um jeito no app e de outro no backend.

export {};
